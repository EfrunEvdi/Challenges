export const simpleAdding = (num) => {
  let result = 0;

  for (let i = 0; i <= num; i++) {
    result = result + i;
  }

  return result;
};

/* 
Verilecek olan tam sayıya kadar olan değerlerin toplanması ( verilen sayı dahil)
*/
